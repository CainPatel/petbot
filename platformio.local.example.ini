; Copy to platformio.local.ini (gitignored). Same password as OTA_PASSWORD_STR
; in esp32/secrets.h, otherwise OTA uploads are refused with "Authentication Failed".
[env:esp32]
upload_flags =
    --timeout=30
    --auth=choose-a-long-random-string
